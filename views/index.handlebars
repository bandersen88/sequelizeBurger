<h1>An enitre web app about burgers</h1>

<h2>Eat Da S Burger</h2>

<ul>
    {{#each burger}}
        {{#unless this.devoured}}
            {{> burger}} 
        {{/unless}}   
    {{/each}}
</ul>

<h2>Ate Da S Burger</h2>

<ul>
    {{#each burger}}
        {{#if this.devoured}}
            {{> burger}} 
        {{/if}}   
    {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burger">Burger Name:</label>
    <input type="text" id="burger" name="name">
  </div>

  <button type="submit">Add Burger</button>
</form>